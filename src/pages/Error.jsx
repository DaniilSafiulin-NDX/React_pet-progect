import React from 'react';

const Error = () => {
    return (
        <div>
            <h1 style={{color: 'red'}}>
                Ну как вам сказать... бюджета на эту страницу не хватило))
            </h1>
        </div>
    );
};

export default Error;
